# Import flask app from flask
from flask import Flask, redirect, url_for, render_template, request

# Create a flask app object
app = Flask(__name__)

# Help me write a function to add 2 strings
def add_strings(str1, str2):
    return str1 + str2


# Write a code to reverse a string
def reverse_string(str1):


# What is the difference between a list and a tuple in Python? Give an example of each.
# A list is a mutable data type whereas a tuple is an immutable data type.